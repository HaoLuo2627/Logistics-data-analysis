车货匹配-车辆数据.txt：
# 结合实际运单后，有效的车辆数据（尤其是最大吨数以及最大方数）。
# 每列分别是：车辆ID、最大吨数、最大方数、车型、车牌、车长（米）。

客户B-提货-车辆优先级.txt:
# 结合所给的数据，从客户B提货而言，每辆车的优先提货城市的推荐（以及权重/频率）。
# 每列分别是：车辆ID、最大吨数、最大方数、车型、车牌、车长（米）、提货、第一优先级（提货地点）、第一优先级权重（频率）、第二优先级（提货地点）、第二优先级权重（频率）、第三优先级（提货地点）、第三优先级权重（频率）。

客户B-卸货-车辆优先级.txt:
# 结合所给的数据，每辆车的优先卸货城市的推荐（以及权重/频率）。
# 每列分别是：车辆ID、最大吨数、最大方数、车型、车牌、车长（米）、第一优先级（卸货地点）、第一优先级权重（频率）、第二优先级（卸货地点）、第二优先级权重（频率）、第三优先级（卸货地点）、第三优先级权重（频率）。

客户A-车辆优先级.txt:
# 结合所给的数据，每辆车的优先卸货城市的推荐（以及权重/频率）（对于A，提货地相同）。
# 每列分别是：车辆ID、最大吨数、最大方数、车型、车牌、第一优先级（卸货地点）、第一优先级权重（频率）、第二优先级（卸货地点）、第二优先级权重（频率）、第三优先级（卸货地点）、第三优先级权重（频率）。